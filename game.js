$(document).ready(function(){

});

(function($){
  var Board = function(){
    // draw the board given the row and col dimensions
    // plant the mines
    // determines if the game is over

  };

  var Game = function(){
    // This deals with everything on the view. Set default values of dimensions, num_bombs
    // Starts a new Game
    // initializes the board
    // triggers the Cheat method by revealing all of the bombs.


  };

  var spot = function(x, y, bomb){
    this.x = x;
    this.y = y;
    this.isMine = bomb;
  }; //

}(jQuery));
